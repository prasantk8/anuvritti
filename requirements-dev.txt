-r requirements.txt
pytest>=8
pytest-cov>=5
httpx2>=2
ruff>=0.6
mypy>=1.11
pyyaml>=6

# Browser rendering is a workstation concern. Production receives filmkit's base
# narration probe from requirements.txt, never Playwright or Chromium.
-e packages/filmkit[chromium]
