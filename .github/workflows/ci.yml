name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# A workflow that can write to the repo is a supply-chain surface. It cannot.
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "22"

jobs:
  # ---------------------------------------------------------------- quality
  lint:
    name: Lint & format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: ruff check
        run: ruff check src tests packages/client/codegen
      - name: ruff format
        run: ruff format --check src tests packages/client/codegen

  types:
    name: Type check (strict)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install -r requirements-dev.txt
      - run: mypy

  # ------------------------------------------------------------------ tests
  test:
    name: Tests & coverage gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: pip install -r requirements-dev.txt
      - name: Install ffmpeg for film rendering
        run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - name: Install the locked JavaScript workspace
        run: npm ci
      # pytest collects tests/design too. `make check` builds this ignored output before
      # either coverage pass; a fresh runner must do the same rather than test an empty dist.
      - name: Build the shared visual world for pytest collection
        run: npm --prefix packages/world run build --silent
      - name: Install the development-only film browser
        run: python -m playwright install --with-deps chromium

      # CLAUDE.md: >= 90% unit. The domain and application layers hold every rule
      # the PRD promises, so they carry the strict gate.
      - name: Core coverage gate (domain + application >= 90%)
        run: |
          pytest --cov=anuvritti.domain --cov=anuvritti.application \
                 --cov-report=term-missing --cov-fail-under=90

      # The local gate is stricter than the framework's 80% integration floor.
      - name: Overall coverage gate (>= 90%)
        run: pytest --cov=anuvritti --cov-report=term-missing --cov-fail-under=90

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: .coverage
          if-no-files-found: ignore

  # ---------------------------------------------- the PRD's own boundaries
  constitution:
    name: Ethical constitution (PRD 8.5, 46, 47, 49)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: The product must not cross a line the founder said it would not cross
        run: pytest tests/constitution tests/architecture -v

  # --------------------------------------- the app, client, and shared visual world
  design:
    name: World, client & app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: pip install -r requirements-dev.txt
      - name: Install the locked JavaScript workspace
        run: npm ci
      - name: Build the shared visual world
        run: npm --prefix packages/world run build --silent
      - name: Verify the design contract
        run: pytest tests/design -q
      - name: Test packages/world
        run: npm --prefix packages/world test --silent
      - name: Refuse generated-client drift
        run: python packages/client/codegen/generate.py --check
      - name: Test packages/client
        run: npm --prefix packages/client test --silent
      - name: Test apps/anuvritti
        run: npm --prefix apps/anuvritti test --silent
      - name: Test memtara.com site
        run: npm --prefix site test --silent
      - name: Verify site constitution
        run: npx tsx scripts/check-site.ts

  # ----------------------------------------------------- the memory compiler package
  filmkit:
    name: Filmkit quality gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: Run filmkit lint, strict types, and coverage
        run: make -C packages/filmkit check PY=python

  # -------------------------------------------------------------- security
  security:
    name: Security
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Audit the locked JavaScript tree
        run: npm run audit
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - run: pip install -r requirements-dev.txt

      - name: Dependency audit
        run: |
          pip install pip-audit
          grep -v "^-e " requirements.txt > /tmp/req.txt
          pip-audit --requirement /tmp/req.txt --strict

      - name: Static application security testing
        run: |
          pip install bandit[toml]
          bandit -r src -ll

      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: No .env committed
        run: |
          if git ls-files --error-unmatch .env >/dev/null 2>&1; then
            echo "::error::.env is committed - PRD 44 requires zero secrets in the repo"
            exit 1
          fi

  # --------------------------------------------------------------- container
  container:
    name: Container build & scan
    runs-on: ubuntu-latest
    needs: [lint, types, test]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: anuvritti:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build probe-free size baseline
        uses: docker/build-push-action@v6
        with:
          context: .
          target: runtime-base
          push: false
          load: true
          tags: anuvritti:ci-probe-free
          cache-from: type=gha

      - name: Build handset audio fixtures
        uses: docker/build-push-action@v6
        with:
          context: .
          target: probe-fixtures
          push: false
          load: true
          tags: anuvritti:ci-probe-fixtures
          cache-from: type=gha

      - name: Prove production media probing and publish its image cost
        run: scripts/verify-production-media-probe.sh anuvritti:ci anuvritti:ci-probe-free anuvritti:ci-probe-fixtures

      - name: Verify the image does not run as root
        run: |
          user="$(docker image inspect anuvritti:ci --format '{{.Config.User}}')"
          echo "image user: ${user:-<empty>}"
          if [ -z "$user" ] || [ "$user" = "root" ] || [ "${user%%:*}" = "0" ]; then
            echo "::error::the image would run as root"
            exit 1
          fi

      - name: Verify the image refuses to start without an encryption key
        run: |
          # PRD 44: production must not run without encryption at rest.
          set +e
          output="$(docker run --rm -e ANUVRITTI_ENV=production anuvritti:ci 2>&1)"
          status=$?
          set -e
          if [ "$status" -ne 78 ]; then
            echo "::error::the image returned $status instead of refusing invalid configuration"
            exit 1
          fi
          if ! grep -q "ANUVRITTI_MEDIA_KEY is required in production" <<<"$output"; then
            echo "::error::the image failed without naming the missing encryption key"
            exit 1
          fi

      - name: Vulnerability scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: anuvritti:ci
          format: table
          exit-code: "1"
          ignore-unfixed: true
          severity: HIGH,CRITICAL

  # ------------------------------------------------------------------ gate
  ci:
    name: CI
    runs-on: ubuntu-latest
    needs: [lint, types, test, constitution, design, filmkit, security, container]
    if: always()
    steps:
      - name: Require every job to pass
        run: |
          if echo '${{ toJSON(needs) }}' | grep -q '"result": *"\(failure\|cancelled\)"'; then
            echo "::error::a required job did not pass"
            exit 1
          fi
          echo "all checks passed"
